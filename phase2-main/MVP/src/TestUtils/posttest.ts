/**
 * This runs a function after tests complete, posttest runs automatically after npm run test
 * @author DSinc
 */
import { showTestMetrics } from '../Processors/testProcessor';
showTestMetrics();
